export interface IOrbitControlsOpt {
  target: { x: number; y: number; z: number }
  startPosition: { x: number; y: number; z: number }
  enableDamping: boolean
  rotateSpeed: number
  enabled: boolean
}
